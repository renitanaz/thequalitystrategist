{
  "mcpServers": {
    "peakandpack-api": {
      "command": "npx",
      "args": ["-y", "@example/peakandpack-mcp-server"],
      "env": {
        "PEAKANDPACK_API_TOKEN": "${PEAKANDPACK_API_TOKEN}"
      }
    }
  }
}
